1: "Load parameters from params.json. Set the simulation mode to PQ and run the simulation using the full method with a grid phase jump disturbance of 0.4 rad. Check the stability, get the PLL settling time, and save the plot results to main_1_results.png.",
2: "Load parameters from params.json, set the simulation mode to PQ, and test a very large grid phase jump disturbance of 1.0 rad. Run the simulation using the full method, check the stability of the system, and plot the results to main_2_results.png.",
3: "Load the inverter parameters and set the simulation mode to Frequency Support (FS) with a frequency droop coefficient K_droop_f of 40.0 and a deadband f_db of 0.0005. Run the full method simulation, check stability, calculate the transient overshoot for active power (P_expr), and save the plot to main_3_results.png.",
4: "Set the simulation mode to VOLT-VAR with K_droop_v of 20.0 and Vdb of 0.01. Apply a voltage sag disturbance by setting Vmag_dist to 0.90, run the simulation, check stability, get the reactive power overshoot (Q_expr), and plot the results to main_4_results.png.",
5: "Load the parameters and run the simulation using both the Full Newton-Raphson and the Schur Complement solvers. Compare the computation times and speedup of the two solvers, and save the plot of the Full method results to main_5_results.png.",
6: "For Volt-Var deadband sensitivity analysis, set the mode to VOLT-VAR with K_droop_v at 20.0 and Vdb at 0.05. Apply a voltage disturbance of 1.025, run the full simulation, check the stability, get the reactive power transient overshoot, and export the plot to main_6_results.png.",
7: "Set the inverter to PQ mode and apply a phase jump disturbance of 0.2 rad. Run the full simulation, check for stability, and calculate the peak transient overshoot for both active power (P_expr) and reactive power (Q_expr). Plot the results to main_7_results.png.",
8: "To test the damping effect of filter time constants, set the simulation mode to PQ, change the Tf_pll parameter to 0.002, and apply a 0.2 rad phase jump disturbance. Run the simulation, check the stability, get the PLL settling time, and save the results plot to main_8_results.png.",
9: "Evaluate weak-grid stability by loading the parameters and multiplying both the R_line_pu and X_line_pu parameters by 5. Set the mode to PQ with a 0.1 rad phase jump, run the simulation using the full method, check stability, get the settling time, and save the plot to main_9_results.png.",
10: "Run a combined disturbance study by setting the simulation mode to PQ and applying both a phase jump of 0.3 rad and a voltage sag (Vmag_dist) of 0.92 simultaneously. Run the full simulation, check stability, calculate the settling time and the overshoot for P_expr and Q_expr, and save the plot to main_10_results.png."
